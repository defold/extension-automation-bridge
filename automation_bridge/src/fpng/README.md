# fpng

`fpng.cpp` and `fpng.h` are vendored unchanged from
[`richgel999/fpng`](https://github.com/richgel999/fpng) commit
`925796543b9d26b8edfcdcecd94c1dac280f29fc` (fpng 1.0.6).

fpng is released into the public domain under the Unlicense. The complete
license text is included at the end of `fpng.cpp`.
